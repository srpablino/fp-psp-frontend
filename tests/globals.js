/* eslint no-unused-vars: 0 */

// Define global variables for test cases with karma.
// These variables are defined with webpack DefinePlugin
// in development mode.
// For now we need to add the globals
// this way to avoid test failures.
window.APP_VERSION = null;
